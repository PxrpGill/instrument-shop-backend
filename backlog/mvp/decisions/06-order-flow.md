# 06 Order Flow

## Decision

Для MVP заказ реализуется как заявка.

## Flow

1. покупатель отправляет заказ;
2. backend создаёт `Order` и `OrderItem`;
3. backend фиксирует цену товара на момент заказа;
4. заказ получает статус `new`;
5. сотрудник меняет статусы вручную.

